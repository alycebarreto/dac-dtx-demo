spring.application.name=jpa-persistencia
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
server.port=8081

# Seleciona a implementacao do DAO: "jpa" (EntityManager), "jdbc" (DataSource), "mongo" (MongoDB) ou "dtx" (transacao distribuida)
app.dao.impl=dtx


# MongoDB Atlas — cluster proprio criado com as credenciais do enunciado
# (cluster do professor rejeitava handshake TLS; este eh um cluster M0 free
#  com o mesmo usuario/senha do enunciado, mantendo a interface igual).
app.mongo.uri=mongodb+srv://ciceroaraujo_db_user:MX4LoFyhjxVGr0Jp@cluster0.xosnyyn.mongodb.net/?appName=Cluster0
# Fallback local Docker (descomente se Atlas estiver instavel):
# app.mongo.uri=mongodb://localhost:27017
app.mongo.database=demo
